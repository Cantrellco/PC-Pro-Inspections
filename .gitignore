# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
.env.*.local

# local secrets (see .env.example)
.env
.env.local
.env*.local

# Playwright MCP session logs
.playwright-mcp/

# Impeccable working artifacts: keep the approved comp + its approval sidecar,
# drop the dealt decision hand, raw generations, and review screenshots (large PNGs).
.impeccable/review/
.impeccable/mocks/decision/
.impeccable/mocks/assets/
.impeccable/mocks/world/
.impeccable/mocks/comps/*
!.impeccable/mocks/comps/comp-3-print-in-progress.png
!.impeccable/mocks/comps/comp-3-print-in-progress.json

# Impeccable mobile proof sheets (large PNG screenshots + inlined HTML sheet):
# same rationale as .impeccable/review/ above — working artifacts, not site content.
.impeccable/mobile/
